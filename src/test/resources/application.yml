spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    user: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    schemas: ${DB_NAME}
